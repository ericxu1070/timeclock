# Copy this file to .env and fill in your own value.
# The app fetches .env at runtime — deploy it alongside index.html.
#
# NOTE: this file is served to the browser, so it is NOT private.
# It only keeps config out of the committed source. Never put real
# secrets (API keys, passwords) here.

# Your Google Apps Script web app deployment URL
# (Deploy → New deployment → Web app → copy the URL)
SCRIPT_URL=
