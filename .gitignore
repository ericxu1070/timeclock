# Local config — copy .env.example to .env and fill in SCRIPT_URL.
# Ignored by default so a deployment URL isn't published by accident.
# (This project's GitHub Pages deploy force-commits .env on purpose, since
# Pages can only serve committed files — see README.)
.env

# Editor/tooling scratch — not part of the app
.claude/
